{% extends 'base.html.twig' %}

{% block body %}
<!-- SECTION SOLUTIONS CENTRÉE VERTICALEMENT -->
<section id="solutions" class="flex-grow-1 d-flex align-items-center bg-light">
  <div class="container text-center py-5">
    <h2 class="fw-bold mb-5">Nos Solutions</h2>
    <div class="row g-4 justify-content-center">

      <!-- Carte 1 : Administration -->
      <div class="col-md-4">
        <a href="{{ path('app_produit', { slug: 'vcm' }) }}" class="text-decoration-none text-dark">
          <div class="card h-100 border-0 shadow-sm">
            <img src="{{ asset('assets/img/v-system/vcm.png') }}" class="card-img-top" alt="Administration">
            <div class="card-body">
              <span class="badge bg-danger mb-2 fs-6">V<small>CM</small></span>
              <h3 class="h5 fw-bold mb-3">Administration</h3>
              <p>Gérez et archivez vos documents en toute sécurité.</p>
            </div>
          </div>
        </a>
      </div>

      <!-- Carte 2 : Entrepôts -->
      <div class="col-md-4">
        <a href="{{ path('app_produit', { slug: 'vsm' }) }}" class="text-decoration-none text-dark">
          <div class="card h-100 border-0 shadow-sm">
            <img src="{{ asset('assets/img/v-system/vsm.png') }}" class="card-img-top" alt="Entrepôts">
            <div class="card-body">
              <span class="badge bg-danger mb-2 fs-6">V<small>SM</small></span>
              <h3 class="h5 fw-bold mb-3">Entrepôts</h3>
              <p>Gérez, organisez et sécurisez vos stocks simplement et efficacement.</p>
            </div>
          </div>
        </a>
      </div>

      <!-- Carte 3 : Industriels -->
      <div class="col-md-4">
        <a href="{{ path('app_produit', { slug: 'vlm' }) }}" class="text-decoration-none text-dark">
          <div class="card h-100 border-0 shadow-sm">
            <img src="{{ asset('assets/img/v-system/vlm.png') }}" class="card-img-top" alt="Industriels">
            <div class="card-body">
              <span class="badge bg-danger mb-2 fs-6">V<small>LM</small></span>
              <h3 class="h5 fw-bold mb-3">Industriels</h3>
              <p>Optimisez vos volumes et maîtrisez vos stocks avec une solution intelligente.</p>
            </div>
          </div>
        </a>
      </div>

    </div>
  </div>
</section>
{% endblock %}
